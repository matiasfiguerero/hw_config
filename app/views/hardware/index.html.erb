<h1>Configuración del Hardware</h1>

<p><strong>Sistema Operativo:</strong> <%= @os %></p>
<p><strong>Nombre del Host:</strong> <%= @hostname %></p>
<p><strong>Número de CPUs:</strong> <%= @cpu_count %></p>
<p><strong>Memoria RAM:</strong> <%= @memory %> GB</p>

<h2>Información del Disco:</h2>
<table border="1">
  <thead>
    <tr>
      <th>Filesystem</th>
      <th>Size</th>
      <th>Used</th>
      <th>Available</th>
      <th>Use%</th>
      <th>Mounted on</th>
    </tr>
  </thead>
  <tbody>
    <% @disk_info.each do |disk| %>
      <% cols = disk.split %>
      <tr>
        <% cols.each do |col| %>
          <td><%= col %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>